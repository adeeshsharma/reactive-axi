# Build output - generated by scripts/build.js (esbuild), not source, never formatted.
dist/

# Generated by scripts/build-plugin.js from package.json via JSON.stringify(..., null, 2) -
# array formatting deliberately differs from Prettier's own (which collapses short arrays
# onto one line), so this stays hand-off.
plugin.json

# Generated by release-please-action from commit history on every release PR - its
# double-blank-line-after-heading template doesn't match Prettier's markdown style,
# and it's never meant to be hand-edited anyway.
CHANGELOG.md
