# Generated by scripts/build-plugin.js from package.json via JSON.stringify(..., null, 2) -
# array formatting deliberately differs from Prettier's own (which collapses short arrays
# onto one line), so this stays hand-off, mirroring lavish-axi's own .prettierignore.
plugin.json
